# Recruitment Backend

This is the backend for the Recruitment System.
It is built with **Node.js** and **Express** and exposes a REST API that the frontend (Vue) communicates with via HTTP (Axios).

---

## Run (Will change later)

node src/server.js
